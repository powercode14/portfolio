<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tistory.powercode.portfolio.mapper.HelloMapper">
    <select id="hello" parameterType="java.util.Map" resultType="string">
        SELECT 'hello ' || #{param} || ' mybatis' FROM DUAL
    </select>
</mapper>